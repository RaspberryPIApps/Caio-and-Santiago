Important files are:

client.py
client_function.py

server.py
server_function.py

config.txt (NOTE: at with v2 only the IP config is being used)

commands.txt (NOTE: not final commands, for example I think the comands
can be: calPosition, getAngle, etc...)

dummyData.txt

---

config_backUp.txt not usefull to you, just so that I know how it looks
in case that the other gets corrupted)

untitles.py might be usefull to you and ciao but not for reprot)

Santiago